<controls:MetroWindow x:Class="SettingWindows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
        xmlns:dockablz="clr-namespace:Dragablz.Dockablz;assembly=Dragablz"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:EUD_Editor_3"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        WindowTitleBrush="{DynamicResource PrimaryHueDarkBrush}"
        Title="{StaticResource Setting}" Height="500" Width="650" WindowStartupLocation="CenterScreen" MinWidth="650" MinHeight="500" Topmost="True">

    <Grid>
        <dragablz:TabablzControl x:Name="MainTab"  FixedHeaderCount="6" TabStripPlacement="Left" Background="{DynamicResource MaterialDesignPaper}" >
            <TabItem x:Name="TabItem_ProjectSetting" Header="{StaticResource Project Setting}" >
                <StackPanel Margin="10">
                    <Label  Content="{StaticResource OpenMap}" HorizontalAlignment="Left" VerticalAlignment="Top"  />
                    <DockPanel Margin="5">
                        <Button DockPanel.Dock="Right" Height="26" Content="{StaticResource Find}" VerticalAlignment="Top" Click="BtnOpenMapSet_Click" Margin="0" />
                        <Button Click="Button_Click" Style="{StaticResource MaterialDesignToolButton}" DockPanel.Dock="Left"  Margin="0,-4" >
                            <materialDesign:PackIcon Kind="FolderOpen"   />
                        </Button>
                        <TextBox x:Name="TBOpenMap"  Foreground="{DynamicResource MaterialDesignBody}"
  materialDesign:TransitionAssist.DisableTransitions="True" IsReadOnly="True"  />
                     
                    </DockPanel>
                  
                    
                    <Separator Background="#4CA0A0A0"/>

                    
                    <Label Content="{StaticResource SaveMap}" HorizontalAlignment="Left" VerticalAlignment="Top"  />
                    <DockPanel Margin="5">
                        <Button DockPanel.Dock="Right" Height="26" Content="{StaticResource Find}" VerticalAlignment="Top" Click="BtnSaveMapSet_Click" />
                        <Button Click="Button_Click_1" Style="{StaticResource MaterialDesignToolButton}" DockPanel.Dock="Left" Margin="0,-4" >
                            <materialDesign:PackIcon Kind="FolderOpen"   />
                        </Button>
                        <TextBox  x:Name="TBSaveMap"  Foreground="{DynamicResource MaterialDesignBody}" 
  materialDesign:TransitionAssist.DisableTransitions="True" IsReadOnly="True" />
                    </DockPanel>

                    
                    <Separator Background="#4CA0A0A0"/>


                    <Label Content="{StaticResource TempFileLoc}" HorizontalAlignment="Left" VerticalAlignment="Top"  />
                    <DockPanel Margin="5">
                        <Button Click="Button_Click_2" Style="{StaticResource MaterialDesignToolButton}" DockPanel.Dock="Left" Margin="0,-4" >
                            <materialDesign:PackIcon Kind="FolderOpen"   />
                        </Button>
                        <ComboBox 
                            SelectionChanged="TempFileCombobox_SelectionChanged"
                            x:Name="TempFileCombobox"
                            MinWidth="80"
                            VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesignBody}">
                            <ComboBoxItem Content="{StaticResource TempFileLoc0}" />
                            <ComboBoxItem Content="{StaticResource TempFileLoc1}"/>
                            <ComboBoxItem Content="{StaticResource TempFileLoc2}"/>
                            <ComboBoxItem Content="{StaticResource TempFileLoc3}"/>
                        </ComboBox>
                        <Button x:Name="TempFilebtn" DockPanel.Dock="Right" Height="26" Content="{StaticResource Find}" VerticalAlignment="Top" Click="TempFilebtn_Click"/>
                        <TextBox x:Name="TempFiletextbox"  Margin="10,0" Foreground="{DynamicResource MaterialDesignBody}" 
  materialDesign:TransitionAssist.DisableTransitions="True" IsReadOnly="True" />
                    </DockPanel>


                    <Separator Background="#4CA0A0A0"/>


                    <Label Content="{StaticResource Setting}" HorizontalAlignment="Left" VerticalAlignment="Top" />
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <CheckBox x:Name="UseCustomTbl" Checked="UseCustomTbl_Checked" Unchecked="UseCustomTbl_Checked" TextBlock.Foreground="{DynamicResource MaterialDesignBody}" Style="{StaticResource MaterialDesignCheckBox}" Content="{StaticResource TBLChange}" Margin="0,0,20,0"/>

                        <CheckBox TextBlock.Foreground="{DynamicResource MaterialDesignBody}" Style="{StaticResource MaterialDesignCheckBox}" Content="{StaticResource AutoCompile}"/>
                    </StackPanel>
                    
                    
                    <Separator Background="#4CA0A0A0"/>


                </StackPanel>
            </TabItem>
            <TabItem Header="{StaticResource Default Setting}">
                <StackPanel Margin="10">
                    
                    <Label Content="{StaticResource StarExeFile}" HorizontalAlignment="Left" VerticalAlignment="Top" />

                    <DockPanel Margin="5">
                        <Button DockPanel.Dock="Right" Grid.Row="2" Grid.Column="2" Height="26" Content="{StaticResource Find}" VerticalAlignment="Top" Click="BtnStarCraftexe" />
                        <TextBox x:Name="TBStarCraftexe" Grid.Row="2" Grid.Column="1" Margin="10,0"  Foreground="{DynamicResource MaterialDesignBody}"
  materialDesign:TransitionAssist.DisableTransitions="True" IsReadOnly="True" />
                    </DockPanel>
                    
                    <Separator Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Background="#4CA0A0A0"/>

                    <Label  Content="{StaticResource euddraftExe}" HorizontalAlignment="Left" VerticalAlignment="Top"  />


                    <DockPanel Margin="5">
                        <DockPanel DockPanel.Dock="top">
                            <Button DockPanel.Dock="Right" Grid.Row="5" Grid.Column="2" Height="26" Content="{StaticResource Find}" VerticalAlignment="Top"  Click="Btneuddraftexe" />
                            <TextBox x:Name="TBeuddraftexe" Grid.Row="5" Grid.Column="1" Margin="10,0"  Foreground="{DynamicResource MaterialDesignBody}"
  materialDesign:TransitionAssist.DisableTransitions="True" IsReadOnly="True" />
                        </DockPanel>
                        <DockPanel DockPanel.Dock="top" LastChildFill="False">
                            <Label x:Name="euddraftVersion1" DockPanel.Dock="Left" HorizontalAlignment="Left" VerticalAlignment="Top"  />
                            <Label x:Name="euddraftVersion2" DockPanel.Dock="Right" HorizontalAlignment="Left" VerticalAlignment="Top"  />
                        </DockPanel>
         
                    </DockPanel>
                    <Separator Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" Background="#4CA0A0A0"/>

                    <Label Grid.Row="7" Grid.Column="1" Content="{StaticResource Language}" HorizontalAlignment="Left" VerticalAlignment="Top" />
                    <ComboBox x:Name="CBLanguage" Grid.Row="8" Grid.Column="1" Margin="5" SelectionChanged="CBLanguage_SelectionChanged" 
                    MinWidth="72"
                    VerticalAlignment="Bottom" Foreground="{DynamicResource MaterialDesignBody}">
                        <ComboBoxItem Content="한국어" Tag="ko-KR"/>
                        <ComboBoxItem Content="English" Tag="us-EN"/>
                    </ComboBox>

                    <Separator Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="2" Background="#4CA0A0A0"/>

                    <Label Content="{StaticResource RegSetting}" HorizontalAlignment="Left" VerticalAlignment="Top" />
                    <StackPanel Margin="5">
                        <DockPanel Width="220" HorizontalAlignment="Left" >
                            <Button DockPanel.Dock="Right" x:Name="exeConnectbtn" Content="{StaticResource RegSettingBtn}" Click="ExeConnectbtn_Click" Margin="30,1,1,1"  Height="26"/>
                            <CheckBox x:Name="regCheck" TextBlock.Foreground="{DynamicResource MaterialDesignBody}" Style="{StaticResource MaterialDesignCheckBox}" Content="{StaticResource RegSettingCheckbox}"  Unchecked="CheckBox_Unchecked" Checked="CheckBox_Checked"/>
                        </DockPanel>
                      
                        <Border  HorizontalAlignment="Left" Width="220"  BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}">
                            <WrapPanel >
                                <CheckBox x:Name="e3scb" Width="100" TextBlock.Foreground="{DynamicResource MaterialDesignBody}"
                                      Style="{StaticResource MaterialDesignCheckBox}" Content="e3s"/>

                                <CheckBox x:Name="e2scb" Width="100" TextBlock.Foreground="{DynamicResource MaterialDesignBody}"
                                      Style="{StaticResource MaterialDesignCheckBox}" Content="e2s"/>

                                <!--<CheckBox x:Name="e2pcb" Width="100" TextBlock.Foreground="{DynamicResource MaterialDesignBody}"
                                      Style="{StaticResource MaterialDesignCheckBox}" Content="e2p"/>-->

                                <!--<CheckBox x:Name="eescb" Width="100" TextBlock.Foreground="{DynamicResource MaterialDesignBody}"
                                      Style="{StaticResource MaterialDesignCheckBox}" Content="ees"/>-->

                                <!--<CheckBox x:Name="memcb" Width="100" TextBlock.Foreground="{DynamicResource MaterialDesignBody}"
                                      Style="{StaticResource MaterialDesignCheckBox}" Content="mem"/>-->
                            </WrapPanel>
                        </Border>
                       
                        <!--e3s e2s e2p ees mem-->
                      

                    </StackPanel>

                    <Separator Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="2" Background="#4CA0A0A0"/>
                    <Label Content="{StaticResource UpdataSetting}" HorizontalAlignment="Left" VerticalAlignment="Top" />


                </StackPanel>
            </TabItem>
            <TabItem Header="{StaticResource Theme}">
                <local:ThemeContorl/>
            </TabItem>

            <TabItem Header="{StaticResource Editor Setting}" >
                <StackPanel Margin="10">
                    <Label  Content="{StaticResource DatEdit Setting}" HorizontalAlignment="Left" VerticalAlignment="Top"  />
                    <DockPanel>
                        <Label Grid.Row="1" Grid.Column="1" Content="{StaticResource Code Lan}" HorizontalAlignment="Left" VerticalAlignment="Top" />
                        <ComboBox Name="CBCodeLan" Grid.Row="2" Grid.Column="1"   SelectionChanged="CBCodeLan_SelectionChanged"
                    MinWidth="72"
                    VerticalAlignment="Bottom" Foreground="{DynamicResource MaterialDesignBody}">
                            <ComboBoxItem Content="{StaticResource Code CEng}"/>
                            <ComboBoxItem Content="{StaticResource Code CKor-Eng}"/>
                            <ComboBoxItem Content="{StaticResource Code CKor-Kor}"/>
                        </ComboBox>

                    </DockPanel>
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <CheckBox x:Name="ChangeTblUse" Checked="ChangeTblUse_Checked" Unchecked="ChangeTblUse_Checked" TextBlock.Foreground="{DynamicResource MaterialDesignBody}" Style="{StaticResource MaterialDesignCheckBox}" Content="{StaticResource Code CTbl}" Margin="0,0,20,0"/>
                        <CheckBox x:Name="TopMostforce" Checked="TopMostforce_Checked"  Unchecked="TopMostforce_Checked" TextBlock.Foreground="{DynamicResource MaterialDesignBody}" Style="{StaticResource MaterialDesignCheckBox}" Content="{StaticResource TopMostOption}" Margin="0,0,20,0"/>

                    </StackPanel>

                    <Separator Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="2" Background="#4CA0A0A0"/>
                    <Label  Content="{StaticResource TE Setting}" HorizontalAlignment="Left" VerticalAlignment="Top"  />
                    <CheckBox x:Name="TopMostTEforce" Checked="TopMostTEforce_Checked"  Unchecked="TopMostTEforce_Checked" TextBlock.Foreground="{DynamicResource MaterialDesignBody}" Style="{StaticResource MaterialDesignCheckBox}" Content="{StaticResource TopMostOption}" Margin="0,0,20,0"/>

                    <Separator Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="2" Background="#4CA0A0A0"/>
                    <Label  Content="{StaticResource Plugin Setting}" HorizontalAlignment="Left" VerticalAlignment="Top"  />
                    <CheckBox x:Name="TopMostPluginforce" Checked="TopMostPluginforce_Checked"  Unchecked="TopMostPluginforce_Checked" TextBlock.Foreground="{DynamicResource MaterialDesignBody}" Style="{StaticResource MaterialDesignCheckBox}" Content="{StaticResource TopMostOption}" Margin="0,0,20,0"/>

                </StackPanel>
            </TabItem>

        </dragablz:TabablzControl>

    </Grid>
</controls:MetroWindow>
