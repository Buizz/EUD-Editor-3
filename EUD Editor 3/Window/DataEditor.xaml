<Window x:Class="DataEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
        xmlns:dockablz="clr-namespace:Dragablz.Dockablz;assembly=Dragablz"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:EUD_Editor_3"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="DataEditor" Height="450" Width="800" WindowStartupLocation="Manual" Topmost="True">
    <Window.Resources>
        <system:String x:Key="Partition">QuickStart</system:String>
    </Window.Resources>



    <DockPanel>
        <Expander Name="Console" DockPanel.Dock="Bottom" Header="명령창">
            <RichTextBox Height="96" >
                <FlowDocument>
                    <Paragraph>
                        <Run Text="RichTextBox"/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
        </Expander>

        <Expander Name="CodeExpander" Header="코드"   ExpandDirection="Right"  DockPanel.Dock="Left">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="100*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="100*"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>

                <ListBox x:Name="CodeIndexerList"  Grid.RowSpan="1" Grid.ColumnSpan="1" Grid.Column="1"/>
                <TreeView x:Name="CodeIndexerTree"  Grid.RowSpan="1" Grid.ColumnSpan="1" Grid.Column="1" Visibility="Hidden">
                    <TreeViewItem Header="Employee1">
                        <TreeViewItem Header="Jesper"/>
                        <TreeViewItem Header="Aaberg"/>
                        <TreeViewItem Header="12345"/>
                    </TreeViewItem>
                    <TreeViewItem Header="Employee2">
                        <TreeViewItem Header="Dominik"/>
                        <TreeViewItem Header="Paiha"/>
                        <TreeViewItem Header="98765"/>
                    </TreeViewItem>
                </TreeView>
                <TextBox Grid.Column="1" HorizontalAlignment="Left" Grid.Row="1" TextWrapping="Wrap"  Width="257"/>
                <StackPanel  Grid.RowSpan="3">
                    <Button Background="White">
                        유닛
                    </Button>
                    <Button Background="White">
                        무기
                    </Button>
                    <Button>
                        비행정보
                    </Button>
                    <Button>
                        스프라이트
                    </Button>
                    <Button>
                        이미지
                    </Button>
                    <Button>
                        업그레이드
                    </Button>
                    <Button>
                        기술
                    </Button>
                    <Button>
                        명령
                    </Button>
                    <Button>
                        버튼셋
                    </Button>
                </StackPanel>
                <StackPanel Grid.Column="1"  Grid.Row="2" Orientation="Horizontal" >
                    <Button x:Name="Btnsortn123" Click="Btn_sortn123" BorderBrush="{x:Null}" IsEnabled="False" Padding="0">
                        <Image  Source="/EUD Editor 3;component/Icon/sort-numeric.png" />
                    </Button>
                    <Button x:Name="BtnsortABC" Click="Btn_sortABC" BorderBrush="{x:Null}" Padding="0">
                        <Image  Source="/EUD Editor 3;component/Icon/sort-alphabetical.png"/>
                    </Button>
                    <Button x:Name="BtnsortTree" Click="Btn_sortTree" BorderBrush="{x:Null}" Padding="0">
                        <Image  Source="/EUD Editor 3;component/Icon/file-tree.png"/>
                    </Button>
                    <CheckBox IsEnabledChanged="Btn_isEdit">
                        수정여부
                    </CheckBox>
                </StackPanel>

            </Grid>


        </Expander>

        <dockablz:Layout  Partition="{StaticResource Partition}">
            <dragablz:TabablzControl Margin="8">
                <dragablz:TabablzControl.InterTabController>
                    <dragablz:InterTabController Partition="{StaticResource Partition}" />
                </dragablz:TabablzControl.InterTabController>
                <TabItem Header="Tab No. 1" IsSelected="True">
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">Hello World</TextBlock>
                </TabItem>
                <TabItem Header="Tab No. 2">
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">We Have Tearable Tabs!</TextBlock>
                </TabItem>
                <TabItem>
                    <!-- with context menu -->
                    <TabItem.Header>
                        <Grid>
                            <Grid.ContextMenu>
                                <ContextMenu>
                                    <!--we'll be in a popup, so give dragablz a hint as to what tab header content needs closing -->
                                    <MenuItem Command="{x:Static dragablz:TabablzControl.CloseItemCommand}"/>
                                </ContextMenu>
                            </Grid.ContextMenu>
                            <TextBlock>Tab No. 3</TextBlock>
                        </Grid>
                    </TabItem.Header>
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">I feel like an ice cold drink</TextBlock>
                </TabItem>
            </dragablz:TabablzControl>
        </dockablz:Layout>



    </DockPanel>
</Window>
